## 1. Remove Global Stats Header

- [x] 1.1 Delete the `.hdr-meta` CSS rule from `public/index.html`
- [x] 1.2 Delete the `<div class="hdr-meta" id="hdrMeta">` element from the HTML header
- [x] 1.3 Delete the `$('hdrMeta').textContent = ...` assignment in `renderAll()`
